{
  "name": "staelfogarti",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "lint": "eslint netlify/functions",
    "format": "prettier --write .",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {
    "resend": "^6.12.0",
    "stripe": "^22.0.2"
  }
}
