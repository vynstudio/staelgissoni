{
  "name": "staelfogarti",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "lint": "eslint netlify/functions",
    "format": "prettier --write .",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {
    "stripe": "^14.0.0",
    "googleapis": "^144.0.0",
    "resend": "^4.0.0"
  }
}
